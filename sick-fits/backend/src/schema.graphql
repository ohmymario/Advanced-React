# import * from './generated/prisma.graphql' 

# Only imports relevant information in schema

type Mutation {
  createItem(
    title:String,
    description: String,
    price: Int,
    image: String,
    largeImage: String
  ): Item!
}

type Query {
  items: [Item]!
}